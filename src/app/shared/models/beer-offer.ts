export interface BeerOffer {
    id: string;
    ownerId: string;
    name: string;
    price: number;
    imageUrl: string;
    description: string;
}
// TODO finish transitioning and merging these two models